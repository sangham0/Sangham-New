---
// Temporary ad-campaign mirror of /counselling-for-meditators.
// Both routes render the same shared component so they can never drift.
// The canonical URL and OG tags point at /counselling-for-meditators/, and
// this route is excluded from the sitemap in astro.config.mjs. A /c4m ->
// /c4m-2 safety redirect lives in vercel.json. Delete this file and both
// redirects when the campaign ends (see docs/DECISIONS.md).
import CounsellingForMeditatorsPage from '../components/CounsellingForMeditatorsPage.astro';
---

<CounsellingForMeditatorsPage />
